meta {
  name: 17 Teacher AI Analytics (200)
  type: http
  seq: 17
}

get {
  url: {{baseUrl}}/courses/{{courseId}}/ai/student-analytics
  auth: none
}

headers {
  Authorization: Bearer {{teacherToken}}
}

script:post-response {
  test("status is 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
