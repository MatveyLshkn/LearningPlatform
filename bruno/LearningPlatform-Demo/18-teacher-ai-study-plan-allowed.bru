meta {
  name: 21 Teacher AI Study Plan (200)
  type: http
  seq: 21
}

get {
  url: {{baseUrl}}/courses/{{courseId}}/students/{{studentId}}/ai-study-plan
  auth: none
}

headers {
  Authorization: Bearer {{teacherToken}}
}

script:post-response {
  test("status is 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
