meta {
  name: 13 Student Get Assessment
  type: http
  seq: 13
}

get {
  url: {{baseUrl}}/assessments/{{assessmentId}}
  auth: none
}

headers {
  Authorization: Bearer {{studentToken}}
}

script:post-response {
  test("status is 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
