meta {
  name: 18 Student AI Analytics (403)
  type: http
  seq: 18
}

get {
  url: {{baseUrl}}/courses/{{courseId}}/ai/student-analytics
  auth: none
}

headers {
  Authorization: Bearer {{studentToken}}
}

script:post-response {
  test("status is 403", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
